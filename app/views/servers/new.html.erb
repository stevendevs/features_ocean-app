<% content_for :title, "New server" %>

<%= turbo_frame_tag :dashboard do %>
  <div class="w-full flex justify-center pt-24">
    <div class="max-w-2xl w-full bg-gray-600 p-4 rounded-lg">
      <h1 class="font-bold text-4xl text-gray-100">New server</h1>

      <%= render "form", server: @server %>

      <%= link_to "Cancel", root_path, class: "ml-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium" %>
    </div>
  </div>
<% end %>