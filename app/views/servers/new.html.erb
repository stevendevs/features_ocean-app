<% content_for :title, "New server" %>
<%= turbo_frame_tag :dashboard do  %>
  <div class="w-full flex justify-center bg-lime-500">
<div class="max-w-2xl w-full ">
  <h1 class="font-bold text-4xl">New server</h1>

  <%= render "form", server: @server %>

  <%= link_to "Back to servers", root_path, class: "w-full sm:w-auto text-center mt-2 sm:mt-0 sm:ml-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium" %>
</div>
</div>
<%  end%>

